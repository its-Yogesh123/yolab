# ──────────────────────────────────────────────────────────
#  YOLAB Python Services — Merged Requirements
#  Combines dependencies from OnePic + Maester
# ──────────────────────────────────────────────────────────

# Core web framework
fastapi>=0.140.0
uvicorn[standard]>=0.52.0
python-multipart>=0.0.20
python-dotenv>=1.1.0

# ── OnePic dependencies ────────────────────────────────────
pillow>=12.0.0
numpy>=2.0.0

# ── Maester dependencies ───────────────────────────────────
# PDF loading — fitz module is shipped by pymupdf, do NOT add 'fitz' here
pymupdf>=1.28.0

# LangChain core + integrations
# Let pip resolve openai transitively via langchain-openai
langchain>=1.3.0
langchain-core>=1.5.0
langchain-community>=0.4.0
langchain-openai>=1.4.0
langchain-huggingface>=1.2.0
langchain-chroma>=1.1.0
langchain-text-splitters>=1.1.0

# Local embeddings
sentence-transformers>=5.0.0
chromadb>=1.0.0

# ChromaDB — local persistent vector store
chromadb>=0.5.0

# Misc
pydantic>=2.13.0
httpx>=0.28.0
